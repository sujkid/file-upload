'use strict';

const onUploadFileSuccess = (/*data*/) => {
  // $('.bookname').val('');
  // $('.bookdesc').val('');
};

const onUploadFileFailure = (/*error*/) => {
};

module.exports = {
  onUploadFileSuccess,
  onUploadFileFailure,
};
